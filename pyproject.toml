[project]
name = "hexlet-django-blog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.7",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "factory-boy>=3.3.3",
]
